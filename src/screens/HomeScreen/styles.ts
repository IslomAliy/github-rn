import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  content: {
    flex: 1,
    marginTop: 10,
    justifyContent: 'center',
  },
  text: {
    justifyContent: 'center',
    color: 'red',
    fontSize: 32,
  },
  noUserText: {
    marginTop: 30,
    alignItems: 'center',
    textAlign: 'center',
  },
  defaultText: {
    marginTop: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
